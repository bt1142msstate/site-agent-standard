{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/bt1142msstate/site-agent-standard/raw/main/spec/0.1/schemas/query.schema.json",
  "$defs": {
    "resource": {
      "type": "object",
      "required": ["id", "description", "visibility", "execution", "modes", "filters"],
      "properties": {
        "id": { "type": "string" },
        "description": { "type": "string", "minLength": 8 },
        "visibility": { "enum": ["public", "authenticated"] },
        "permissionsAll": { "type": "array", "items": { "type": "string" } },
        "permissionsAny": { "type": "array", "items": { "type": "string" } },
        "execution": { "enum": ["local", "host"] },
        "modes": { "type": "array", "minItems": 1, "uniqueItems": true, "items": { "type": "string" } },
        "filters": { "type": "object", "additionalProperties": { "type": "object" } },
        "sorts": { "type": "array", "uniqueItems": true, "items": { "type": "string" } },
        "maxResults": { "type": "integer", "minimum": 1, "maximum": 1000 },
        "resultSchema": { "type": "object" },
        "destinationId": { "type": "string" }
      },
      "patternProperties": { "^x-[a-z0-9._-]+$": true },
      "additionalProperties": false
    }
  }
}
